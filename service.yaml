apiVersion: v1
kind: Service
metadata:
  name: web-site
  labels:
    app: web-site
    tier: frontend
spec:
  type: NodePort
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
    nodePort: 32321
  selector:
    app: web-site
    tier: frontend
